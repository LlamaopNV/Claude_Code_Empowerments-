---
description: An example slash command — replace with your own. Invoked as /example-empowerment:example-command.
---

# Example Command

This is a template slash command shipped with the `example-empowerment` plugin.

When invoked, do the following with the user's input (`$ARGUMENTS`):

1. Summarize what the user asked for in one sentence.
2. Explain that this is a placeholder command and should be replaced.

To make your own command, copy this file into `commands/`, rename it (the
filename without `.md` becomes the command name), and rewrite the frontmatter
`description` and the body instructions.
